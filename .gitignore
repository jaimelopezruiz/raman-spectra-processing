__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/

# Microscope images acquired alongside the spectra: kept locally, not in git
# (see README "Input data"). Raw spectra (input/*.csv/.txt) and params/*.xlsx
# ARE tracked — the input spectra are the measured source data and are NOT
# reproducible, so they stay in the repo.
input/**/*.czi
input/**/*.bmp
input/**/*.png
# Generated results: every file here is reproducible from the input spectra +
# configs via the pipeline (python main.py <file>), so it is not tracked.
# NOTE: this means the paper's "data deposited in repo [49]" claim now rests on
# the deposited code + configs (+ raw input spectra), not on committed result
# CSVs. Archive a canonical results set with the tagged release if a frozen,
# citable copy of the numbers is required (see paper-tracker.md G).
output/
*.docx
Paper WIP Draft.docx
paper-tracker.md
Chemical Disorder Final.xlsx
Cross Spectrum Ratios original.xlsx
# Paper-working data (this folder was renamed from "Stress Mapping/"): stress-map
# point spectra + χ workbook, kept local, not in the public repo.
Temporal Claude Context/
MEETING NOTES 22_07_2026.txt

# Manuscript drafts (all versions) stay local
*.docx
notesondiffs.md
zenodo-setup.md
